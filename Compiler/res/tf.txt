programa teste;

var int, entrada:inteiro;
       b:booleano;

funcao a:inteiro;
inicio
   a := 1 + 4*2+(2*(((4+4)))) + 1;
fim;
inicio
   int:= 1 + 4*2+(2*(((4+4))));
   entrada := 4;
   b := verdadeiro;
   escreva(int);
   leia(entrada);
   se b
   entao
   inicio
     escreva(entrada);
     int := (a + 1)*2;
     escreva(int);
   fim
   senao
     escreva(int);

   enquanto entrada > 0 faca
   inicio
     escreva(entrada);
     entrada := entrada - 1;
   fim
fim.
